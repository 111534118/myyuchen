# Tasks: Add Guide Search Function

1.  **[Spec]** Finalize and approve the spec deltas for `site-navigation` and `search-functionality`.
2.  **[Code]** Create new empty files: `src/main/resources/static/game/search-results.html` and `src/main/resources/static/game/search.js`.
3.  **[HTML]** Add the search form HTML to the navigation bar in `index.html`, `main-quest.html`, and `map.html`.
4.  **[HTML]** Create the basic structure for the results page in `search-results.html`, including a container for results.
5.  **[JavaScript]** Implement the search form handling logic in `search.js` to redirect to the results page with a query parameter.
6.  **[JavaScript]** Implement the results page logic in `search.js` to fetch content, parse it, search for the query, and display results dynamically.
7.  **[Code]** Link `search.js` in all four HTML files (`index.html`, `main-quest.html`, `map.html`, `search-results.html`).
8.  **[Styling]** Add CSS to `style.css` for the new search form and search results to match the site's theme.
9.  **[Validation]** Manually test the search functionality with various keywords, including ones that should and should not produce results.
